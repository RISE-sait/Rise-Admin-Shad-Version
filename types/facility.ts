export interface Facility {
    id: string;
    name: string;
    Address: string
    updated_at?: string | Date;
    updatedAt?: string | Date;
  }

  export interface FacilityType {
    id: string;
    name: string;
  }
